name: Security analysis

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * 1'

jobs:
  security:
    uses: dvsa/.github/.github/workflows/php-security.yaml@promote-php-actions
    secrets:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}